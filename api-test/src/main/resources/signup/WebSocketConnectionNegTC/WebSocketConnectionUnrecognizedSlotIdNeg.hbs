{
  "slotId": "{{slotId}}",
  "idvSlotAllotted": "{{idvSlotAllotted}}",
  "sendDestination": "{{sendDestination}}",
  "subscribeSlotId": "__unrecognized_test_slot_id_not_allotted__",
  "expectSubscriptionRejected": true,
  "message1": {
    "slotId": "{{slotId}}",
    "stepCode": "START",
    "frames": [
      {{#each message1.frames}}
      { "frame": "{{this.frame}}", "order": "{{this.order}}" }{{#unless @last}},{{/unless}}
      {{/each}}
    ]
  },
  "message2": {
    "slotId": "{{slotId}}",
    "stepCode": "liveness_check",
    "frames": [
      {{#each message2.frames}}
      { "frame": "{{this.frame}}", "order": "{{this.order}}" }{{#unless @last}},{{/unless}}
      {{/each}}
    ]
  }
}
