{
  "verifiedTransactionID": "{{verifiedTransactionID}}"{{#unless omitField}},
  "field": "{{field}}"{{/unless}}{{#unless omitFileParts}},
  "filePath": [{{#each filePath}}"{{this}}"{{#unless @last}},{{/unless}}{{/each}}],
  "fileKeyName": "{{fileKeyName}}"{{/unless}}
}
