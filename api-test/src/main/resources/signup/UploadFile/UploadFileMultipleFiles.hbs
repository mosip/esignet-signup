{
  "verifiedTransactionID": "{{verifiedTransactionID}}",
  "field": "{{field}}",
  "filePath": [{{#each filePath}}"{{this}}"{{#unless @last}},{{/unless}}{{/each}}],
  "fileKeyName": "{{fileKeyName}}"
}
