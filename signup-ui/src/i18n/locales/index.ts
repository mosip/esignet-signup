import { ResourceLanguage } from "i18next";

import { en } from "./en";
import { km } from "./km";

export const locales = {
  km: km as unknown as ResourceLanguage,
  en: en as unknown as ResourceLanguage,
};
